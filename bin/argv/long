#!/usr/bin/env node

// example of configuring custom long option names
var options = {always: '--colorize', never: '--colorize'};
var ttycolor = require('../..')(options);
ttycolor.defaults();
console.info('an %s message', 'info');
console.error('an %s message', 'error');
