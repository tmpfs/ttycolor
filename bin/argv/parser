#!/usr/bin/env node

// this configuration is an example of declaring a custom parser
function parser(modes, option, argv) {
  return modes.always;
}

var ttycolor = require('../..')(parser);
ttycolor.defaults();
console.info('an %s message', 'info');
console.error('an %s message', 'error');
